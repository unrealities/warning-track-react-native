import { StyleSheet } from "react-native";

export const BackgroundStyles = StyleSheet.create({
  container: {
    bottom: 0,
    flexGrow: 1,
    left: 0,
    position: "absolute",
    right: 0,
    top: 0,
  },
});
