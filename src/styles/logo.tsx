import { StyleSheet } from "react-native";

export const LogoStyles = StyleSheet.create({
  logo: {
    alignContent: "center",
    justifyContent: "center",
    height: 60,
    minWidth: 60,
    resizeMode: "contain",
  },
  logoContainer: {
    flexDirection: "row",
    height: 60,
    justifyContent: "center",
    width: 60
  },
  mlbTVContainer: {
    padding: 8,
  },
});
