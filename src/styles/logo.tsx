import { StyleSheet } from 'react-native';

export const LogoStyles = StyleSheet.create({
    logo: {
        alignContent: 'center',
        justifyContent: 'center',
        height: 60,
        minWidth: 60,
        resizeMode: 'contain',
        width: '100%'
    },
    logoContainer: {
        height: '100%',
        marginHorizontal: 8,
        width: 60
    },
    mlbTVContainer: {
        padding: 8
    }
});
